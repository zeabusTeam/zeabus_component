# in directory mode we have spilt directory that make for future
# In the future will have adding proposal
add_library( Convert SHARED 
    bytes.cpp
    vector/one_byte.cpp
    string/integer_type.cpp
    integer/string_type.cpp
)
